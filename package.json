{
  "name": "financas-familia",
  "version": "0.2.0",
  "private": true,
  "description": "Finanças da Família — PWA de controle financeiro familiar (Supabase + GitHub Pages)",
  "type": "module",
  "scripts": {
    "test": "node --test \"tests/js/**/*.test.js\"",
    "dev": "npx --yes serve@14 -l 3000 ."
  },
  "engines": {
    "node": ">=20"
  }
}
